# 피드백 적용 현황 — 2026-09-17

## 기획자 피드백 2차 — 2026-09-18 통합 재검토

이전 세션이 만든 작업 트리를 `5066bb4`(checkpoint)로 먼저 커밋해 되돌아갈 지점을 남겼다. 그 위에서 워크트리 환경(생성된 Prisma 클라이언트 없음, 메인 체크아웃을 가리키는 `node_modules/node_modules` 심볼릭 링크, `.env` 없음)을 고치고, 전체 프런트엔드 테스트 356개·Go 테스트 24개 패키지·정적 빌드를 통과시켰다. 로컬 :8080 API 는 2차 라우트가 없는 옛 바이너리였으므로(`/api/schedules/batch` 404) 현재 소스로 다시 빌드해 재시작했다. 1차에서 이미 반영한 항목(포인트 충전, 상단 마이 제거, 프로필 사진·알림, 학습 2×2, 키워드 정오답, 단계 버튼 이름, 의미 단위 답안, 구조도, 자료 선택 폴더화)은 제외한다.

| 항목 | 상태 | 근거와 경계 |
|---|---|---|
| 1. 메인·취약노트 폴더 정리, 지금 복습할 항목 빨간 표시 | 반영 (이전 세션) + 이번 확인 | 카드 보관함·문제은행·오답노트·자료함이 과목 폴더로 시작한다. 폴더 행의 `지금 복습 N`/`다시 확인 N`은 `--danger` 색 뱃지이며 숫자·문구를 함께 둔다. 오답노트 폴더 안에서는 이번에 각 문제에 출처 자료명(예: `3. 항상성과 몸의 조절 · 틀린 기록 1회`)을 붙여 어디에 분류됐는지 보이게 했다. 실제 390px 브라우저에서 카드/오답노트 폴더 진입 확인 |
| 2. 블라인드 복습 주기, 가림 편집의 박스 삭제·지우개 | 반영 (이전 세션) | 가림 카드는 다른 카드와 같은 복습 설정을 쓰며 작성 화면에 그 사실과 설정 위치를 적었다. 편집기는 박스·이동·펜·지우개 4도구, 선택 삭제, 50단계 실행 취소/다시 실행, AI 가림 위치 찾기. 브라우저 자동 조작은 파일 선택기를 열 수 없어 `tests/occlusion-edit.test.ts` 와 이전 세션의 캡처(`.unlazy/feedback-2/occlusion-edit.png`)를 근거로 둔다 |
| 3. AI 카드 만들기, 기억률 % 설정, 플래시카드의 핵심/자세히 | 반영 (이전 세션) | 카드 보관함 상단 `AI로 카드 만들기`(자료 기반 생성). 복습 설정은 가볍게/균형 있게/꼼꼼하게 프리셋 + 세부 목표 기억률이며, 1·3·7일의 비율 조절이 아님을 화면에 설명한다. 1·3·7시간 일괄 환산은 카드별 기억 모델과 어긋나 채택하지 않았다. 해설 길이 설정은 문제풀이 전용이고 플래시카드 답에는 적용하지 않는다고 명시 |
| 4. 문제풀이 폴더화, 폴더 먼저 만들고 안에서 추가 | 반영 (이전 세션) + 이번 테스트 정리 | 문제은행은 폴더 → 범위·개수 → 시작. 자료함은 `새 과목 폴더` 를 만들면 그 폴더로 들어가고 `이 폴더에 자료 추가` 로 올린다. 옛 목록 UI 를 전제로 하던 테스트 5개를 폴더 우선 동작에 맞춰 고쳤다 |
| 5. 개념 연결, 설정에서 배운 과목 선택, 성취기준 | 반영 (이전 세션) + 이번 설정 진입 추가 | 해설 아래 `개념을 연결해 봐요`: 앞서/다음 개념과 배운 과목 여부. 2022 개정 고등학교 성취기준 1,486개(109과목)를 서버에 내장해 생성 프롬프트의 학습 배경으로 쓴다(파인튜닝 아님). `설정 > 내 계정` 에 `배운 과목` 행을 추가해 마이 탭 외에서도 닿는다 |
| 6. 헷갈리면 4가지, Again 연동, 결과 화면에서도 4가지 | 반영 (이전 세션) + 이번 결과 화면 컨트롤 교체 | 해설·오답노트·전체 결과에 플래시카드 / 서술형 도우미 / 비슷한 문제 더 풀기 / 커뮤니티에 물어보기. 실제 브라우저에서 플래시카드를 누르자 `다시 상자 · 첫 복습` 카드로 즉시 열렸다(당일 복습). 결과 화면의 문제 선택은 네이티브 select 대신 공용 OptionField 로 바꿨다. 비슷한 문제·서술형 생성은 OpenRouter 키가 있는 서버에서만 동작하며 이번 검증에서 유료 호출은 하지 않았다 |
| 7. 시간표 방식(에타·투두메이트) | 기존 유지 | 사용자가 두 방식 모두 좋다고 했으므로 현재 일간 카드형 시간표를 유지했다. 주간 격자 뷰는 만들지 않았다 |
| 8. 일정 기간 설정(한 학기), 에타식 추가 | 반영 (이전 세션) | 일정 추가에서 `매주 반복` → 요일, 종료일, `4주`/`한 학기 · 16주` 프리셋, 등록 날짜 미리보기, `POST /api/schedules/batch` 원자적 등록. 최대 183일·200개 |
| 9. 고정 일정은 두고 변동 후보를 넣어 가능한 안 추천 | 반영 (이전 세션) | `이번 주 계획 세우기`: 할 일(총 시간·회당 시간·요일)과 `시간표 후보 비교`(학원 후보 묶음). 고정·기존 일정을 장애물로 두고 규칙 기반으로 최대 9안을 계산하며 사용자가 고른 안만 등록한다. AI 라고 부르지 않는다 |
| 10. 마켓 | 제외 | 사용자가 MVP·필수가 아니라고 했으므로 만들지 않았다 |

### 이번 검증

- `npm run typecheck`, `npm test`(356 통과·0 실패), `npm run build`(WEB_PUBLISH_OK), `go build/vet`, `go test ./...`(24 패키지 ok). 간격 토큰 검사가 2차 신규 CSS 5개 파일의 raw px 에서 실패하던 것을 토큰으로 바꿔 통과시켰다.
- 실제 390px 인앱 브라우저(체험 학생 계정, 로컬 :3000 + 재빌드한 :8080): 학습 2×2, 카드 보관함 폴더·드릴다운, 오답노트 폴더·출처 자료명, 문제은행 폴더→설정→풀이→해설(개념 연결·4행동)→결과 화면, 플래시카드 담기, 일정 추가의 매주 반복·16주, 이번 주 계획의 할 일·후보 비교, 설정의 배운 과목 행, 나에게 맞는 복습 프리셋.
- 하지 않은 것: 실제 모바일 기기, 운영 배포, 유료 AI 호출(비슷한 문제·서술형 생성·AI 가림 제안의 실제 결과), 이미지 파일 업로드가 필요한 가림 편집의 브라우저 조작.


현재 작업 트리와 기존 검증 기록을 대조했다. 아래의 **이번 수정·검증**과 **기존 반영**은 구분한다. 기존 반영 항목을 이번 작업에서 모든 기기·운영 환경까지 다시 검증했다는 뜻은 아니다. 실제 결제 활성화는 미완료 상태로 남긴다.

## 이번에 찾아서 고친 누락

| 피드백 / 발견된 문제 | 수정 | 확인 근거 |
|---|---|---|
| 홈에도 가로로 긴 과목·자료 관리 버튼 요청. 학습 화면에만 들어가 있었음 | 홈의 복습 영역 아래에 추가. 홈·학습에서 동일한 공용 컴포넌트 사용 | 390px 홈 캡처, 클릭 → `/subjects` → 뒤로 가기 |
| 최근 자료 ‘전체 보기’가 자료함이 아닌 학습 메뉴로 이동 | `/subjects`로 연결 | 실제 홈 → 전체 보기 → 자료함 확인 |
| 빈 자료함에서 과목 관리 → 추가를 다시 찾아야 함 | ‘첫 자료 올리기’가 과목 생성·자료 등록을 바로 시작. 학기 필터 결과만 비었으면 ‘전체 학기 보기’ | 빈 계정에서 두 진입점 확인 |
| 자료함 만들기 → 자료 올리기에서 과목을 생성하면 작업이 끝남 | 공용 자료 등록 흐름으로 묶어 과목 저장 후 입력창으로 연결 | POST subjects 201 → 자료 입력 → POST materials 201 |
| 사진 등록이 과목 목록의 마지막 항목을 임의 선택 | 한 과목일 때만 바로 열고 여러 과목이면 명시적으로 선택 | 순서 독립 테스트, 생명과학/수학 선택 화면과 카메라 우선 입력 확인 |
| 과목 없는 직접 카드 작성이 자료함 안내에서 끊김 | 과목 생성 의도를 전달하고 생성된 과목을 선택한 카드 편집기로 연결 | 별도 빈 계정: 과목 생성 → 카드 저장 → 한국사 보관함 1장 |

취소한 등록 의도는 URL에서 정리하여 새로고침 때 다시 열리지 않게 했다. `SourceUploadFlow`를 첫 학습·자료함·카메라 진입에서 재사용한다. 보더리스 면과 기존 간격 토큰을 유지했다.

## 기획자 피드백 1–7

| 항목 | 상태 | 근거와 경계 |
|---|---|---|
| 1. 학부모 응원 포인트 충전 결제 | **실결제 활성화 미완료** | 1원=1P, 주문/서버 승인/멱등 적립/오류 화면 구현. 현재 `TOSS_CLIENT_KEY`, `TOSS_SECRET_KEY`, `PAYMENTS_LIVE` 미설정. 결제사 확정·키 설정·실제 결제/취소 검증 필요. [설정 안내](PLANNER_FEEDBACK_REVIEW.md) |
| 2. 중복 상단 마이페이지 제거 | 기존 반영 | 홈 헤더에는 검색·알림만 있음. 커뮤니티 내 활동은 별도 기능으로 유지 |
| 3. 프로필 사진 / 학부모 댓글·자녀 학습 알림 | 기존 반영 | [기능·권한·검증 기록](PROFILE_FEEDBACK_REFERENCES.md). 이번에는 프런트엔드 회귀 검사. 사진 업로드·부모 알림의 실제 동작은 기존 기록에 구분 |
| 4. 학습 네 가지 진입점 | 반영 + 후속 보완 | 학습 콘텐츠가 있으면 2×2, 첫 사용은 자료 추가 → 학습 만들기. [온보딩 설계](STUDY_ONBOARDING_REFERENCES.md). 홈의 자료 관리 누락은 이번 수정 |
| 5. 키워드 정답·오답·놓친 정답 표시 | 기존 반영 | 개수 제한, 확인 전 비공개, 확인 후 아이콘·문구·면으로 표시. [검토](ESSAY_FEEDBACK_REFERENCES.md), 관련 회귀 테스트 |
| 6-1. 단계 이동 버튼 이름 | 기존 반영 | 다음 단계로 넘어가기 / 이전 단계로 돌아가기. 제출은 별도 행동 |
| 6-2. 긴 답안을 의미 단위로 나누기 | 기존 반영 | 문장·문단 경계로 표시하고 원문을 보존. [검토](ESSAY_FEEDBACK_REFERENCES.md) |
| 6-3. 작성 중 힌트와 채점 후 구조도 | 기존 반영 | 같은 구조도 컴포넌트를 두 위치에서 사용. [검토](ESSAY_FEEDBACK_REFERENCES.md) |
| 7. 자료 선택 폴더화 | 기존 반영 + 등록 동선 보완 | 과목 폴더·최근 자료·검색·정렬·빈 상태·여러 자료 선택. 이번에 폴더 진입 전 과목/자료 등록의 끊김 수정 |

## 이어진 요청

| 요청 | 현재 반영 근거 |
|---|---|
| 서술형 질문/입력 위계, 하단 만들기, 자료 선택 먼저, 단계와 선택 수 구분 | [서술형 생성 재설계](ESSAY_CREATION_REDESIGN.md), [단계/선택 UI](GENERATION_PROGRESS_SELECTION_REVIEW.md), `.unlazy/essay-composer/GATES.md` |
| 자료 선택 높이, 다중 자료/주제, 결과 원문 추적 | `.unlazy/planner-feedback/GATES.md` R7–R9 및 자료 선택 회귀 검사 |
| 과목의 카드 탭 내부 목록과 카드 보관함 | [카드 보관함](CARD_LIBRARY_REVIEW.md), 이번 수동 카드 결과 보관함 확인 |
| 호버/버튼 위계와 보더리스 시스템 | `.unlazy/action-states/GATES.md`, 이번 새 진입점도 기존 공용 면/상태 사용 |
| 폴링·실시간 갱신·당겨서 새로고침 | [갱신 정책](REFRESH_AND_SURFACES.md), `.unlazy/live-refresh/GATES.md` |
| 시간표 입력/학교 시간/초안/시트 | [학교 등록](SCHOOL_REGISTRATION_REVIEW.md), [시간표](PLANNER_FOCUS_REVIEW.md) |
| 커뮤니티 탭·검색·글쓰기·첨부·댓글·쪽지 | [커뮤니티 흐름](COMMUNITY_FAMILIAR_REVIEW.md), [내비게이션](COMMUNITY_NAVIGATION_REVIEW.md), 관련 프런트엔드 회귀 검사 |
| 세션 만료 시 전역 정리와 로그인 복귀 | [세션 경계](SESSION_EXPIRY_REVIEW.md), 세션 경계 회귀 검사 |
| 로컬 3000 유지 | 기존 Next dev 유지. 이번 변경은 로컬에 반영했으며 새 운영 배포/메인 병합은 수행하지 않음 |

초기 `.unlazy/community-v3-frontier` / `community-v3-editor`의 미체크 목록은 이후 변경된 글쓰기·커뮤니티 구조를 전제로 한 옛 항목도 포함한다. 이를 현재 미구현 기능이나 이번 새 검증 성공으로 집계하지 않는다. 후속 사용자 요청에 맞춘 `community-familiar`·`editor-tools` 등의 기록을 함께 참조한다.

## 이번 검증

`.unlazy/feedback-closeout/GATES.md`에 타입 검사, 학습/홈 집중 테스트, 전체 프런트엔드 테스트, 정적 배포 빌드와 브라우저 증거를 기록한다. 실제 로컬 API를 사용하는 임시 계정 2개로 확인했다. 홈·자료 등록은 390×844, 별도 카드 작성 탭은 1280×720 브라우저에서 확인했으며 가로 넘침과 콘솔 오류는 없었다. 실제 모바일 기기 카메라, 운영 결제, 운영 배포 성공은 이번 결과에 포함하지 않는다.
