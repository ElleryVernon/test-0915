# 피드백 적용 현황 — 2026-09-17

## 기획자 피드백 2차 — 2026-09-18 통합 재검토

이전 세션이 만든 작업 트리를 `5066bb4`(checkpoint)로 먼저 커밋해 되돌아갈 지점을 남겼다. 그 위에서 워크트리 환경(생성된 Prisma 클라이언트 없음, 메인 체크아웃을 가리키는 `node_modules/node_modules` 심볼릭 링크, `.env` 없음)을 고치고, 전체 프런트엔드 테스트 356개·Go 테스트 24개 패키지·정적 빌드를 통과시켰다. 로컬 :8080 API 는 2차 라우트가 없는 옛 바이너리였으므로(`/api/schedules/batch` 404) 현재 소스로 다시 빌드해 재시작했다. 1차에서 이미 반영한 항목(포인트 충전, 상단 마이 제거, 프로필 사진·알림, 학습 2×2, 키워드 정오답, 단계 버튼 이름, 의미 단위 답안, 구조도, 자료 선택 폴더화)은 제외한다.

| 항목                                                       | 상태                                          | 근거와 경계                                                                                                                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------- | --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1. 메인·취약노트 폴더 정리, 지금 복습할 항목 빨간 표시     | 반영 (이전 세션) + 이번 확인                  | 카드 보관함·문제은행·오답노트·자료함이 과목 폴더로 시작한다. 폴더 행의 `지금 복습 N`/`다시 확인 N`은 `--danger` 색 뱃지이며 숫자·문구를 함께 둔다. 오답노트 폴더 안에서는 이번에 각 문제에 출처 자료명(예: `3. 항상성과 몸의 조절 · 틀린 기록 1회`)을 붙여 어디에 분류됐는지 보이게 했다. 실제 390px 브라우저에서 카드/오답노트 폴더 진입 확인                                 |
| 2. 블라인드 복습 주기, 가림 편집의 박스 삭제·지우개        | 반영 (이전 세션)                              | 가림 카드는 다른 카드와 같은 복습 설정을 쓰며 작성 화면에 그 사실과 설정 위치를 적었다. 편집기는 박스·이동·펜·지우개 4도구, 선택 삭제, 50단계 실행 취소/다시 실행, AI 가림 위치 찾기. 브라우저 자동 조작은 파일 선택기를 열 수 없어 `tests/occlusion-edit.test.ts` 와 이전 세션의 캡처(`.unlazy/feedback-2/occlusion-edit.png`)를 근거로 둔다                                  |
| 3. AI 카드 만들기, 기억률 % 설정, 플래시카드의 핵심/자세히 | 반영 (이전 세션)                              | 카드 보관함 상단 `AI로 카드 만들기`(자료 기반 생성). 복습 설정은 가볍게/균형 있게/꼼꼼하게 프리셋 + 세부 목표 기억률이며, 1·3·7일의 비율 조절이 아님을 화면에 설명한다. 1·3·7시간 일괄 환산은 카드별 기억 모델과 어긋나 채택하지 않았다. 해설 길이 설정은 문제풀이 전용이고 플래시카드 답에는 적용하지 않는다고 명시                                                           |
| 4. 문제풀이 폴더화, 폴더 먼저 만들고 안에서 추가           | 반영 (이전 세션) + 이번 테스트 정리           | 문제은행은 폴더 → 범위·개수 → 시작. 자료함은 `새 과목 폴더` 를 만들면 그 폴더로 들어가고 `이 폴더에 자료 추가` 로 올린다. 옛 목록 UI 를 전제로 하던 테스트 5개를 폴더 우선 동작에 맞춰 고쳤다                                                                                                                                                                                  |
| 5. 개념 연결, 설정에서 배운 과목 선택, 성취기준            | 반영 (이전 세션) + 이번 설정 진입 추가        | 해설 아래 `개념을 연결해 봐요`: 앞서/다음 개념과 배운 과목 여부. 2022 개정 고등학교 성취기준 1,486개(109과목)를 서버에 내장해 생성 프롬프트의 학습 배경으로 쓴다(파인튜닝 아님). `설정 > 내 계정` 에 `배운 과목` 행을 추가해 마이 탭 외에서도 닿는다                                                                                                                           |
| 6. 헷갈리면 4가지, Again 연동, 결과 화면에서도 4가지       | 반영 (이전 세션) + 이번 결과 화면 컨트롤 교체 | 해설·오답노트·전체 결과에 플래시카드 / 서술형 도우미 / 비슷한 문제 더 풀기 / 커뮤니티에 물어보기. 실제 브라우저에서 플래시카드를 누르자 `다시 상자 · 첫 복습` 카드로 즉시 열렸다(당일 복습). 결과 화면의 문제 선택은 네이티브 select 대신 공용 OptionField 로 바꿨다. 비슷한 문제·서술형 생성은 OpenRouter 키가 있는 서버에서만 동작하며 이번 검증에서 유료 호출은 하지 않았다 |
| 7. 시간표 방식(에타·투두메이트)                            | 기존 유지                                     | 사용자가 두 방식 모두 좋다고 했으므로 현재 일간 카드형 시간표를 유지했다. 주간 격자 뷰는 만들지 않았다                                                                                                                                                                                                                                                                         |
| 8. 일정 기간 설정(한 학기), 에타식 추가                    | 반영 (이전 세션)                              | 일정 추가에서 `매주 반복` → 요일, 종료일, `4주`/`한 학기 · 16주` 프리셋, 등록 날짜 미리보기, `POST /api/schedules/batch` 원자적 등록. 최대 183일·200개                                                                                                                                                                                                                         |
| 9. 고정 일정은 두고 변동 후보를 넣어 가능한 안 추천        | 반영 (이전 세션)                              | `이번 주 계획 세우기`: 할 일(총 시간·회당 시간·요일)과 `시간표 후보 비교`(학원 후보 묶음). 고정·기존 일정을 장애물로 두고 규칙 기반으로 최대 9안을 계산하며 사용자가 고른 안만 등록한다. AI 라고 부르지 않는다                                                                                                                                                                 |
| 10. 마켓                                                   | 제외                                          | 사용자가 MVP·필수가 아니라고 했으므로 만들지 않았다                                                                                                                                                                                                                                                                                                                            |

### 판정 모델 통합 배포 (2026-09-18, 2차)

- 검증: `node scripts/delivery-check.mjs` → MEMORYZ_DELIVERY_OK, 회귀 검사 REGRESSION_OK(17개 스위트), Go 전체 테스트 0 실패, 프런트 389개 통과. 커밋 `f635111` 기준.
- 배포: 시크릿 `TYPESAFE_API_KEY`(버전 1, 실행 서비스 계정 접근 권한) 생성 → 이미지 `server:f635111f8ed4`(linux/amd64, 37 MB) → 마이그레이션 잡 pending 0·시드 성공 → Cloud Run 리비전 `memoryz-00022-mb2`(`AI_JUDGE=shadow`, `TYPESAFE_MODEL=jev-latest`) → `node scripts/cloud-smoke.mjs` CLOUD_SMOKE_OK(14개 검사) → 공개 주소에서 데모 세션으로 `POST /api/essay/judge` 200(0.24~0.68초, 개념 4/4·임시 100점) → 알림 정책 6개·업타임 확인.
- shadow 모드: 판정을 기록하고 표시 전용 빠른 판정만 제공하며 채점·검수 경로는 바꾸지 않는다. `on` 전환은 `scripts/deploy.mjs`의 `JUDGE_MODE`를 바꾸고 `service`·`verify-service`를 다시 실행하는 한 단계이며, 그 전에 `AiRun` retries의 `judge` 기록으로 운영 일치율을 읽는다.

### 통합 검증과 배포 (2026-09-18)

- 통합 검증: `node server/scripts/regression.mjs` → REGRESSION_OK (17개 스위트, 실패 0; 계약 검사 188·267·119, 자료 37, 캐시 29, 텔레메트리 17, 지터 17), `node scripts/delivery-check.mjs` → MEMORYZ_DELIVERY_OK. 커밋 `9b46112` 기준.
- 배포: 이미지 `server:9b4611235717`(linux/amd64, 37 MB, 정적 웹 포함) → 마이그레이션 잡 pending 0·시드 성공 → Cloud Run 리비전 `memoryz-00021-hcj` → `node scripts/cloud-smoke.mjs` CLOUD_SMOKE_OK(14개 검사, https://memoryz.kr) → 2차 라우트 4개가 공개 주소에서 401(세션 필요)로 응답 → 업타임·알림 정책 6개 확인. 실결제 활성화(TOSS 키)와 유료 AI 호출 검증은 이번 배포에 포함하지 않는다.

### 입력란 포커스 테두리 통일 (2026-09-18)

| 발견                                                               | 원인                                                                                                                                                                                                                                                                                                                                                          | 수정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 이번 주 계획 시트의 입력란만 포커스 테두리 색이 다름 (사용자 질문) | 시트 모듈이 스크롤 영역 안의 버튼·입력란 전체에 자기 포커스 링(2px `--brand-strong` 진한 주황, 3px 간격)을 정의했고, 앱 전체에도 텍스트 입력 포커스가 여러 갈래였다: 검은 링(온보딩·포인트 충전·커뮤니티 검색), 1px/1.5px/2px 주황 테두리·아웃라인(`.field`, 전역 input, 글쓰기 제목, 교육과정 검색), 안쪽 그림자(시간 상자), 강조색 아웃라인(학교 시간 입력) | 토큰 `--focus-ring: 2px solid var(--text-primary)`·`--focus-ring-offset: -2px`(컨트롤 안쪽) 하나로 통일. 바깥쪽 링은 찾고 싶은 공부 검색창처럼 페이지 여백을 넘어 잘렸으므로(사용자 보고) 안쪽에 그려 어떤 컨테이너에서도 잘리지 않게 했다. 전역 input/textarea/select와 `.field`, 옵션 필드, 글쓰기, 검색류, 시간 상자, 학교 시간 입력, 모듈 검색·입력 15곳이 같은 링을 쓴다. 시트 모듈의 규칙은 버튼에만 남긴다. 공용 `.search-field` 래퍼(찾고 싶은 공부·학교 검색)는 원래 링이 전혀 없었으므로 같은 링을 받는다. `tests/focus-ring.test.ts`가 모든 텍스트 입력 포커스 규칙을 훑어 다른 색을 거부한다 |

### 소프트 키보드가 올라온 화면 (2026-09-18)

| 발견                                                                                                   | 원인                                                                                                                                                                                                                                                                                                                                                  | 수정                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 온보딩에서 키보드가 올라오면 헤더가 상태 표시줄 밑으로 밀리고 검색 결과·주 행동이 가려짐 (사용자 보고) | iOS는 키보드가 떠도 레이아웃 뷰포트를 바꾸지 않고 시각 뷰포트만 줄인 뒤 입력란이 보이도록 패닝한다(iPhone 17 Pro·iOS 26 실측: 레이아웃 714, 보이는 높이 377, 패닝 290.66, `window.innerHeight` 423). `100dvh` 세로 컬럼과 `position: fixed; bottom: 0` 막대는 그 사실을 모른 채 키보드 뒤로 남고, 설치형 웹앱의 상태 표시줄 여백도 홈 화면만 처리했다 | 시각 뷰포트를 CSS 변수로 공개하는 하나의 모델(`src/lib/keyboard-inset.ts`: `--keyboard-inset`·`--vv-height`·`--vv-top`·`--safe-top`, `html[data-keyboard=open]`; 열림 판정은 레이아웃 높이 대비 손실, 인셋은 패닝을 뺀 나머지). 하단 고정 막대·시트·토스트는 인셋만큼 올라가고 시트는 보이는 높이로 줄며, 탭 바와 글쓰기 버튼은 타이핑 중 숨고, 고정 헤더는 시각 뷰포트 위 가장자리를 따라간다. 키보드가 열리면 포커스된 입력란을 자기 스크롤 컨테이너(시트 본문 등) 안에서 드러낸다(문서는 브라우저에 맡김). 온보딩은 시각 뷰포트를 따라가는 고정 컬럼으로 바꿔 헤더는 상태 표시줄 아래, 내용은 안에서 스크롤, 푸터는 키보드 위에 남는다. 타이핑 중 단계 표시는 접히고 학교 검색 중에는 (비활성인) 푸터 대신 결과가 공간을 쓴다. 지우기 버튼은 포커스를 유지한다 |
| 학교 검색 중 학년 행과 검색란이 오른쪽으로 삐져나감 (같은 화면에서 발견)                               | WebKit이 `fieldset`에 주는 min-content 최소 너비: iOS 글꼴에서 학년 버튼 4개의 min-content가 열 너비를 넘고, 재배치 시점에 그 최소값이 적용된다                                                                                                                                                                                                       | 전역 `fieldset { min-inline-size: 0 }`, 학년 격자는 `minmax(0, 1fr)`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

검증: `tests/keyboard-inset.test.ts`(모델·드러내기 계산), `tests/keyboard-layout.test.ts`(globals.css의 모든 하단 고정 블록이 인셋을 따르는지 부정 대조 포함), `tests/onboarding-layout.test.ts`; iOS 시뮬레이터(iPhone 17 Pro, iOS 26.5 Safari, 소프트 키보드)에서 온보딩 학교 검색(결과 3개가 키보드 위에 보임, 선택 후 복귀), 찾고 싶은 공부 검색(탭 바 숨김), 이번 주 계획 시트의 할 일 제목 입력(시트가 보이는 높이로 줄고 입력란이 드러남)을 확인. 기록: `.unlazy/feedback-3-keyboard/GATES.md`. 설치형(홈 화면 추가) 웹앱과 실제 기기는 이번 확인에 포함하지 않는다.

### 이번 주 계획 시트 재설계와 로컬 출처 오류 (2026-09-18)

| 발견                                                             | 원인                                                                                                                                                                                                 | 수정                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 이번 주 계획 시트의 UI/UX가 나쁨 (사용자 판단)                   | 긴 단일 폼: 할 일마다 드롭다운 2개·요일·토글이 다 펼쳐지고, 네이티브 `select`/`time` 입력, 비활성 이유가 버튼 아래에 숨음, 결과가 날짜별 목록뿐                                                      | Mobbin 13개 화면을 확인해 다시 설계([참고 기록](FEEDBACK2_PLANNER_REFERENCES.md#이번-주-계획-시트-재설계-2026-09-18-2차)). 언제(시작일 칩·시간대 타일) → 할 일(접힌 행, 누르면 편집기; `한 번에 × 주 N번`; 지금 복습할 카드 기준 빠른 추가 칩) → 후보 비교(선택·접힘) → 고정 푸터(실시간 요약 + 주 행동 + 비활성 이유). 결과는 배치안 칩·3숫자 요약·요일 7행 미리보기·`조건 바꾸기 / 등록하기`. 앱 컨트롤만 사용하며 `tests/planner-workspace-ui.test.ts`가 지킨다 |
| 완료 체크를 해제하면 "허용되지 않은 요청 출처예요" (사용자 보고) | 프리뷰를 `localhost:3000`으로 열면 브라우저 출처가 `APP_URL`(127.0.0.1:3000)과 달라 Go 서버의 CSRF 규칙이 모든 변경 요청을 거절. 개발 프록시가 요청 Host를 바꾸므로 같은 호스트 예외도 통하지 않았음 | 앱 출처가 루프백일 때만 `localhost`·`127.0.0.1`·`::1`을 같은 포트·스킴에서 한 출처로 본다(`httpx.loopbackAlias`, `csrf_test.go`). 운영 출처(https://memoryz.kr)에는 영향 없음. 프리뷰 API 재시작 후 localhost 출처 PATCH가 404(일정 없음)로 통과, 외부 출처는 403 유지                                                                                                                                                                                             |

### 통합 검증에서 고친 것 (2026-09-18)

사용자가 직접 찾은 결함 1건과 회귀 스위트가 잡은 결함을 같이 고쳤다.

| 발견                                                                                                        | 원인                                                                                                                                                                   | 수정                                                                                                                   |
| ----------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 이번 주 계획 시트가 아래에서 잘려 `가능한 요일`·후보 비교·`가능한 배치안 보기`에 닿을 수 없음 (사용자 보고) | 작업 공간형 시트(`.sheet-content-workspace`)는 본문을 `overflow: hidden`으로 고정하고 안쪽이 스크롤을 맡는 구조인데, 이번 주 계획의 격자에는 스크롤 컨테이너가 없었다  | `.workspace`에 `min-height: 0; overflow-y: auto`. 700×1000·375×812에서 끝 버튼까지 닿는 것을 브라우저에서 측정         |
| `sqlc diff` 불일치                                                                                          | 2차 마이그레이션 뒤 생성 코드 미갱신                                                                                                                                   | `sqlc generate`                                                                                                        |
| Prisma 미러 불일치(테이블 8개·컬럼·FK 동작)                                                                 | 2차 마이그레이션 9–15가 미러에 없음                                                                                                                                    | 마이그레이션을 적용한 임시 DB를 `prisma db pull`로 읽어 미러를 다시 만들고 관계 이름만 정리. `MIGRATIONS_MATCH_PRISMA` |
| staticcheck 10건, 단일 사용 래퍼 1건                                                                        | 미사용 타입·상수·함수, 오류 문장 끝 마침표, 숫자 상태 코드                                                                                                             | 제거·정리. 학교 검색 캐시 상수는 실제로 사라진 캐시라 지터 사이트 등록도 함께 삭제                                     |
| config-safety·auth-http 실패                                                                                | `.env`에 새로 들어온 Kakao·Naver 키가 검사용 서버 환경으로 새어 들어가 공급자 목록이 달라짐                                                                            | 검사 스크립트가 소셜 공급자 키를 명시적으로 비움                                                                       |
| api-contract·jitter-http의 생성 422/502                                                                     | 품질 검수(`AI_QUALITY_REVIEW`)가 켜지면 모델이 `citationIds`로 근거 블록을 가리켜야 하는데 가짜 모델은 검수 도구에 답할 수 없음                                        | 가짜 모델 하네스는 검수를 끄고 본문 인용 형식을 유지. 검수 자체는 Go 테스트가 다룬다                                   |
| 계약 스크립트의 옛 응답 형태                                                                                | 게시글·댓글·메시지 뷰에 필드가 늘고, 팔로우가 같은 학년 기본값·역할 검사로 바뀌고, 메시지가 50개 페이지·`before=` 커서로 바뀌고, 학교 검색이 내장 NEIS 디렉터리로 바뀜 | 계약을 현재 동작에 맞게 갱신(학교 검색은 디렉터리 검색으로 다시 씀)                                                    |
| 댓글·게시글·메시지·자료 출처·결제 응답의 시각이 밀리초 자릿수를 잘라 ISO 계약(`.000Z`)을 가끔 어김          | 새 뷰 구조체가 `time.Time`을 그대로 직렬화                                                                                                                             | `jsonx.Time`으로 통일                                                                                                  |
| 빈 `userId`로 팔로우하면 404                                                                                | 검증이 빠져 조회로 바로 넘어감                                                                                                                                         | 입력 검증 복원(400)                                                                                                    |

### 이번 검증

- `npm run typecheck`, `npm test`(356 통과·0 실패), `npm run build`(WEB_PUBLISH_OK), `go build/vet`, `go test ./...`(24 패키지 ok). 간격 토큰 검사가 2차 신규 CSS 5개 파일의 raw px 에서 실패하던 것을 토큰으로 바꿔 통과시켰다.
- 실제 390px 인앱 브라우저(체험 학생 계정, 로컬 :3000 + 재빌드한 :8080): 학습 2×2, 카드 보관함 폴더·드릴다운, 오답노트 폴더·출처 자료명, 문제은행 폴더→설정→풀이→해설(개념 연결·4행동)→결과 화면, 플래시카드 담기, 일정 추가의 매주 반복·16주, 이번 주 계획의 할 일·후보 비교, 설정의 배운 과목 행, 나에게 맞는 복습 프리셋.
- 하지 않은 것: 실제 모바일 기기, 운영 배포, 유료 AI 호출(비슷한 문제·서술형 생성·AI 가림 제안의 실제 결과), 이미지 파일 업로드가 필요한 가림 편집의 브라우저 조작.

현재 작업 트리와 기존 검증 기록을 대조했다. 아래의 **이번 수정·검증**과 **기존 반영**은 구분한다. 기존 반영 항목을 이번 작업에서 모든 기기·운영 환경까지 다시 검증했다는 뜻은 아니다. 실제 결제 활성화는 미완료 상태로 남긴다.

## 이번에 찾아서 고친 누락

| 피드백 / 발견된 문제                                                   | 수정                                                                                           | 확인 근거                                                         |
| ---------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 홈에도 가로로 긴 과목·자료 관리 버튼 요청. 학습 화면에만 들어가 있었음 | 홈의 복습 영역 아래에 추가. 홈·학습에서 동일한 공용 컴포넌트 사용                              | 390px 홈 캡처, 클릭 → `/subjects` → 뒤로 가기                     |
| 최근 자료 ‘전체 보기’가 자료함이 아닌 학습 메뉴로 이동                 | `/subjects`로 연결                                                                             | 실제 홈 → 전체 보기 → 자료함 확인                                 |
| 빈 자료함에서 과목 관리 → 추가를 다시 찾아야 함                        | ‘첫 자료 올리기’가 과목 생성·자료 등록을 바로 시작. 학기 필터 결과만 비었으면 ‘전체 학기 보기’ | 빈 계정에서 두 진입점 확인                                        |
| 자료함 만들기 → 자료 올리기에서 과목을 생성하면 작업이 끝남            | 공용 자료 등록 흐름으로 묶어 과목 저장 후 입력창으로 연결                                      | POST subjects 201 → 자료 입력 → POST materials 201                |
| 사진 등록이 과목 목록의 마지막 항목을 임의 선택                        | 한 과목일 때만 바로 열고 여러 과목이면 명시적으로 선택                                         | 순서 독립 테스트, 생명과학/수학 선택 화면과 카메라 우선 입력 확인 |
| 과목 없는 직접 카드 작성이 자료함 안내에서 끊김                        | 과목 생성 의도를 전달하고 생성된 과목을 선택한 카드 편집기로 연결                              | 별도 빈 계정: 과목 생성 → 카드 저장 → 한국사 보관함 1장           |

취소한 등록 의도는 URL에서 정리하여 새로고침 때 다시 열리지 않게 했다. `SourceUploadFlow`를 첫 학습·자료함·카메라 진입에서 재사용한다. 보더리스 면과 기존 간격 토큰을 유지했다.

## 기획자 피드백 1–7

| 항목                                        | 상태                       | 근거와 경계                                                                                                                                                                                               |
| ------------------------------------------- | -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. 학부모 응원 포인트 충전 결제             | **실결제 활성화 미완료**   | 1원=1P, 주문/서버 승인/멱등 적립/오류 화면 구현. 현재 `TOSS_CLIENT_KEY`, `TOSS_SECRET_KEY`, `PAYMENTS_LIVE` 미설정. 결제사 확정·키 설정·실제 결제/취소 검증 필요. [설정 안내](PLANNER_FEEDBACK_REVIEW.md) |
| 2. 중복 상단 마이페이지 제거                | 기존 반영                  | 홈 헤더에는 검색·알림만 있음. 커뮤니티 내 활동은 별도 기능으로 유지                                                                                                                                       |
| 3. 프로필 사진 / 학부모 댓글·자녀 학습 알림 | 기존 반영                  | [기능·권한·검증 기록](PROFILE_FEEDBACK_REFERENCES.md). 이번에는 프런트엔드 회귀 검사. 사진 업로드·부모 알림의 실제 동작은 기존 기록에 구분                                                                |
| 4. 학습 네 가지 진입점                      | 반영 + 후속 보완           | 학습 콘텐츠가 있으면 2×2, 첫 사용은 자료 추가 → 학습 만들기. [온보딩 설계](STUDY_ONBOARDING_REFERENCES.md). 홈의 자료 관리 누락은 이번 수정                                                               |
| 5. 키워드 정답·오답·놓친 정답 표시          | 기존 반영                  | 개수 제한, 확인 전 비공개, 확인 후 아이콘·문구·면으로 표시. [검토](ESSAY_FEEDBACK_REFERENCES.md), 관련 회귀 테스트                                                                                        |
| 6-1. 단계 이동 버튼 이름                    | 기존 반영                  | 다음 단계로 넘어가기 / 이전 단계로 돌아가기. 제출은 별도 행동                                                                                                                                             |
| 6-2. 긴 답안을 의미 단위로 나누기           | 기존 반영                  | 문장·문단 경계로 표시하고 원문을 보존. [검토](ESSAY_FEEDBACK_REFERENCES.md)                                                                                                                               |
| 6-3. 작성 중 힌트와 채점 후 구조도          | 기존 반영                  | 같은 구조도 컴포넌트를 두 위치에서 사용. [검토](ESSAY_FEEDBACK_REFERENCES.md)                                                                                                                             |
| 7. 자료 선택 폴더화                         | 기존 반영 + 등록 동선 보완 | 과목 폴더·최근 자료·검색·정렬·빈 상태·여러 자료 선택. 이번에 폴더 진입 전 과목/자료 등록의 끊김 수정                                                                                                      |

## 이어진 요청

| 요청                                                                    | 현재 반영 근거                                                                                                                               |
| ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- |
| 서술형 질문/입력 위계, 하단 만들기, 자료 선택 먼저, 단계와 선택 수 구분 | [서술형 생성 재설계](ESSAY_CREATION_REDESIGN.md), [단계/선택 UI](GENERATION_PROGRESS_SELECTION_REVIEW.md), `.unlazy/essay-composer/GATES.md` |
| 자료 선택 높이, 다중 자료/주제, 결과 원문 추적                          | `.unlazy/planner-feedback/GATES.md` R7–R9 및 자료 선택 회귀 검사                                                                             |
| 과목의 카드 탭 내부 목록과 카드 보관함                                  | [카드 보관함](CARD_LIBRARY_REVIEW.md), 이번 수동 카드 결과 보관함 확인                                                                       |
| 호버/버튼 위계와 보더리스 시스템                                        | `.unlazy/action-states/GATES.md`, 이번 새 진입점도 기존 공용 면/상태 사용                                                                    |
| 폴링·실시간 갱신·당겨서 새로고침                                        | [갱신 정책](REFRESH_AND_SURFACES.md), `.unlazy/live-refresh/GATES.md`                                                                        |
| 시간표 입력/학교 시간/초안/시트                                         | [학교 등록](SCHOOL_REGISTRATION_REVIEW.md), [시간표](PLANNER_FOCUS_REVIEW.md)                                                                |
| 커뮤니티 탭·검색·글쓰기·첨부·댓글·쪽지                                  | [커뮤니티 흐름](COMMUNITY_FAMILIAR_REVIEW.md), [내비게이션](COMMUNITY_NAVIGATION_REVIEW.md), 관련 프런트엔드 회귀 검사                       |
| 세션 만료 시 전역 정리와 로그인 복귀                                    | [세션 경계](SESSION_EXPIRY_REVIEW.md), 세션 경계 회귀 검사                                                                                   |
| 로컬 3000 유지                                                          | 기존 Next dev 유지. 이번 변경은 로컬에 반영했으며 새 운영 배포/메인 병합은 수행하지 않음                                                     |

초기 `.unlazy/community-v3-frontier` / `community-v3-editor`의 미체크 목록은 이후 변경된 글쓰기·커뮤니티 구조를 전제로 한 옛 항목도 포함한다. 이를 현재 미구현 기능이나 이번 새 검증 성공으로 집계하지 않는다. 후속 사용자 요청에 맞춘 `community-familiar`·`editor-tools` 등의 기록을 함께 참조한다.

## TypeSafe Jev 평가와 통합

판정 모델을 제품에 넣었다(`AI_JUDGE=off|shadow|on`, 기본 off): 서술형 채점의 별도 검수 호출을 판정 일치로 대체하고, 생성 검수를 판정으로 선별하며, 서술형 화면은 채점을 기다리는 동안 `POST /api/essay/judge`의 빠른 판정으로 핵심 개념 표시를 먼저 보여 준다. LLM 경로·통합 경로·판정 단독의 비교표와 측정 방법은 [TypeSafe Jev 평가](TYPESAFE_JEV_EVALUATION.md)에 있고, 구조는 [AI 아키텍처](AI_ARCHITECTURE.md)의 판정 모델 절에 있다. 운영 배포에서는 `shadow`로 일치율을 먼저 기록한다.

생성이 아닌 판정 호출(서술형 채점의 키워드 상태·유형·지시 삽입, 객관식 독립 풀이, 키워드/방해어 근거)을 실제 API로 측정했다. 답안 유형 98%, 근거 판정 100%, 지시 삽입 감지 100%, 요청 p50 0.27초·전체 $0.018. 권장 순서와 통합 계획은 [TypeSafe Jev 평가](TYPESAFE_JEV_EVALUATION.md)에 있다. 제품 코드는 바꾸지 않았다.

## 이번 검증

`.unlazy/feedback-closeout/GATES.md`에 타입 검사, 학습/홈 집중 테스트, 전체 프런트엔드 테스트, 정적 배포 빌드와 브라우저 증거를 기록한다. 실제 로컬 API를 사용하는 임시 계정 2개로 확인했다. 홈·자료 등록은 390×844, 별도 카드 작성 탭은 1280×720 브라우저에서 확인했으며 가로 넘침과 콘솔 오류는 없었다. 실제 모바일 기기 카메라, 운영 결제, 운영 배포 성공은 이번 결과에 포함하지 않는다.
