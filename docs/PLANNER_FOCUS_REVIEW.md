# 시간표 집중 점검

2026-09-16 · 로컬 127.0.0.1:3000 · 학생 체험 공간. 기존 일정 카드와 빈 시간 표시 구조를 유지했다.

## 발견과 수정

| 상황 | 발견한 문제 | 수정 |
| --- | --- | --- |
| 시작 변경 | 16–17시 일정의 시작을 18시로 바꾸면 18–17시가 됨 | 현재 길이를 유지해 18–19시로 이동. 당일 끝을 넘으면 23:59까지로 제한하고 안내 |
| 밤늦은 추가 | 한 시간을 담을 수 없으면 17–18시로 돌아가는 기본값 | 남은 25분·5분 슬롯을 차례로 찾으며 과거로 돌아가지 않음 |
| 빈 시간 | ‘채우기’가 수동 추가 대신 AI 요청을 즉시 시작 | 빈 구간은 날짜·시작·종료를 채운 직접 추가. 추천은 별도 명칭의 버튼 |
| 짧은 공부 | 25분 학습 선택지는 있지만 60분 미만 빈 구간은 숨김 | 직접 추가할 빈 구간은 25분부터 표시. 추천 가능 시간은 기존 60분 이상 기준을 유지 |
| 입력 이탈 | 작성 중 닫기 후 이름·시간·과목이 사라짐 | 방문 기록에 초안을 보존하고 이어쓰기 제공. 새로고침·학습 탭 왕복 복원 |
| 저장과 새로고침 | 서버 저장 직후 새로고침하면 이미 저장한 초안이 남음 | 서버 일정과 제목·날짜·시간·종류·과목이 일치하면 초안 정리. 저장 응답을 받은 뒤 화면 갱신만 실패하면 재전송 대신 다시 불러오기 |
| 날짜 이동 | 주 이동은 스와이프나 키보드를 알아야 발견 가능 | 이전 주·다음 주 버튼과 주 범위 표시 추가 |
| 달력 | 시간표 월 선택은 월요일, 편집 날짜 선택은 일요일 시작 | 시간표의 편집 달력도 월요일 시작. 다른 화면의 기본 달력은 기존 동작 유지 |
| 시간 정밀도 | 23:59 등을 시간 선택창에서 열면 5분 단위로 반올림 | 현재 시각은 그대로 열고 정확한 분도 선택지에 유지 |
| 반복 고정 일정 | 선택한 날짜보다 앞선 같은 주 날짜까지 추가될 수 있음 | 선택일과 오늘보다 이른 날짜 제외. 시작일·마지막 날짜·개별 일정 저장임을 안내 |
| 삭제 | 편집에서 삭제를 누르면 바로 서버 삭제 | 날짜·시간·제목, 해당 날짜 한 건이라는 범위를 확인. 취소/뒤로가기는 편집 유지 |
| 늦은 추천 | 기다리다 날짜를 바꾸거나 편집해도 결과가 날짜·화면을 바꿈 | 현재 날짜와 열린 편집 상태를 확인. 다른 작업 중이면 준비 알림만 남기고 명시적으로 열기 |
| 추천 설명 | AI 요약의 시간과 실제 제안 블록의 시간이 다를 수 있음 | 실제 제안 블록의 개수·합산 공부 시간을 표시. 빈 시간을 모두 채운다고 표현하지 않음 |

## 실제 브라우저 검증

- 수정 전 18:00–17:00 역전과 작성 초안 유실을 재현했다.
- 다음 주로 이동해 9/23을 선택한 고정 일정 설정에서 9/23–9/27, 수·목·금 3일만 대상으로 표시됨을 확인했다. 반복 저장은 실행하지 않았다.
- 오늘로 돌아와 학교 종료 뒤 16:00–22:00 빈 구간을 누르면 16:00–17:00 직접 추가가 열렸다.
- 이름을 입력하고 시작을 18시로 옮겨 종료가 19시로 따라오는 것을 확인했다. 25분·생명과학Ⅰ을 선택한 뒤 닫기→새로고침→학습 탭→시간표→이어쓰기에서 제목·18:00–18:25·과목이 복원됐다.
- 실제 저장 후 완료를 눌러 25분/1개, 취소하여 0분/0개로 돌아왔다.
- 같은 일정을 15:00으로 편집하여 학교와 25분 겹침 및 저장 비활성화를 확인했다. 제안된 16:00–16:25 이동으로 해결하고 날짜를 9/17로 바꿔 저장했다. 시간표도 9/17로 이동했다.
- 삭제 확인에서 9/17·16:00–16:25·검증 일정 제목과 단일 일정 범위를 확인했다. 브라우저 뒤로가기로 삭제 없이 편집으로 돌아왔다.
- 1시간 일정의 시작을 23:30으로 옮겨 23:59 종료와 29분 표시, 시간 선택창의 정확한 23:59를 확인했다. 이 경계값은 서버에 저장하지 않았다.
- 최종 검증 일정은 9/17 16:25–16:50으로 저장했다. 앞에 남은 16:00–16:25 빈 구간이 표시됐고 누르면 9/17의 16:00–16:25 새 일정이 열렸다. 저장 직후 새로고침하여 남았던 초안은 최종 빌드에서 서버 저장값과 대조해 사라졌으며, 저장 일정은 한 건으로 유지됐다.
- 추천 요청을 9/18에서 시작한 뒤 오늘로 이동하고 직접 추가·달력을 열었다. 추천이 완료돼도 9/16 편집을 유지했다. 닫자 9/18 추천 준비 알림이 나타났고, 보기 버튼으로만 9/18 결과를 열었다. 추천 안은 저장하지 않았다.
- 390px에서 달력을 관찰했고 문서와 대화상자의 폭이 390px, 가로 넘침이 없음을 확인했다.

## 검증 경계와 데이터

자동 검사는 시간 이동·23시 경계·25분 빈 구간·날짜 비교·월요일 달력·반복 시작일·서버 일정/초안 일치 조건과 기존 회귀 검사를 포함한다. 실제 네트워크 장애 주입, 반복 일정 일괄 저장, 영구 삭제, 운영 배포, 모바일 OS 키보드와 터치 제스처는 수행하지 않았다. 추천의 학습 효과나 시간 추정 정확도를 사용자 연구로 검증한 것은 아니다.

체험 공간에 `시간표 점검 · 개념 복습` 일정 1개를 추가해 수정했다. 최종 값은 **9/17 16:25–16:50, 생명과학Ⅰ, 미완료**다. 기존 일정을 삭제하지 않았고 추천 결과의 일정은 저장하지 않았다.
